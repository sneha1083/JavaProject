package Interface_study;

public interface Airtel_Sim {
	public abstract void m1();
	public abstract void m2();
	int i = 20;
	//variable inside interface are by default static and final
	//static which means java interface can not be instantiated by their own
	//final means its value is constant it cannot be re-assigned in program.

}
