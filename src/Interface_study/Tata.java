package Interface_study;

public interface Tata {
	void wireless();
	void cramerApplication();

}

//A concrete method means, the method has complete definition but it can be overridden in the inherited class.
//A class will implements an interface. A class will extends another class. 
//An interface extends another interface.
//No. Interfaces does not allow constructors.
//The variables inside interfaces are static final variables means constants and we can not create object 
//for interface.
//an interface is not extended by a class, it is implemented by a class.
//an interface can extend multiple interface.



